import React from 'react';

export default function <%= entityName %>Detail({ item }) {
  return (
    <div className="w-full max-w-md mx-auto p-4 bg-white rounded shadow">
      <h2 className="text-xl font-bold mb-4">Detail <%= entityName %></h2>
      <p className="mb-2">ID: {item.id}</p>
      <p className="mb-2">Name: {item.name}</p>
    </div>
  );
}
