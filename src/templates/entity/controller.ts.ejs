import { Controller, Get, Post, Body } from "@nestjs/common";
import { <%= entityName.charAt(0).toUpperCase() + entityName.slice(1) %>Service } from "./<%= entityName %>.service";
import { <%= entityName.charAt(0).toUpperCase() + entityName.slice(1) %> } from "./<%= entityName %>.model";

@Controller("<%= entityName %>")
export class <%= entityName.charAt(0).toUpperCase() + entityName.slice(1) %>Controller {
  constructor(private readonly service: <%= entityName.charAt(0).toUpperCase() + entityName.slice(1) %>Service) {}

  @Get()
  getAll() {
    return this.service.findAll();
  }

  @Post()
  create(@Body() data: <%= entityName.charAt(0).toUpperCase() + entityName.slice(1) %>) {
    return this.service.create(data);
  }
}